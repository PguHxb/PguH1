为什么利用多个域名来存储网站资源会更有效？
解：1、节省带宽。网站中内容其实指的就是js,img,css,html，视频等东西。那么当用户访问的时候，如果带有自己的cookie去访问网站，除了需加载自己的内容之外，那么公共部分的内容也是需要加载的，所以这不部分带宽就浪费了
2.节约主域名的的连接数，从而提高用户客户端的网络带宽的利用率，优化页面响应，因为老的浏览器（IE6是典型），针对同一个域名只允许同时保持两个HTTP连接。将图片等资源请求分配到其他域名上，避免了大图片之类的并不一定重要的内容阻塞住主域名上其他后续资源的连接（比如ajax请求）
3.CDN缓存更为方便。